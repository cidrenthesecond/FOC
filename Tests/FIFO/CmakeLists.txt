add_executable(test_FIFO

    AllTests.c
    FIFO_Test.c
    FIFO_TestRunner.c

    ${CMAKE_SOURCE_DIR}/Src/FIFO.c
)

target_include_directories(test_FIFO PRIVATE
    ${CMAKE_SOURCE_DIR}/Inc
)

target_link_libraries(test_FIFO PRIVATE
    unity_lib
)

add_custom_command(TARGET test_FIFO POST_BUILD

    COMMAND test_FIFO -v

    COMMENT "Running FIFO tests..."

)