add_executable(test_CMSIS

    AllTests.c
    CMSIS_Test.c
    CMSIS_TestRunner.c
)

target_link_libraries(test_CMSIS PRIVATE
    CMSISDSP
    unity_lib
)

add_custom_command(TARGET test_CMSIS POST_BUILD

    COMMAND test_CMSIS -v

    COMMENT "Running CMSIS tests..."

)